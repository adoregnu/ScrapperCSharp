<UserControl x:Class="Scrapper.View.MediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Scrapper.View"
             xmlns:player="clr-namespace:Scrapper.View.MediaPlayer"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:iex="http://schemas.microsoft.com/expression/2010/interactions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

        <i:Interaction.Triggers>
            <iex:KeyTrigger Key="Esc">
                <i:InvokeCommandAction Command="{Binding CmdEscPress}" />
            </iex:KeyTrigger>
        </i:Interaction.Triggers>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <local:FileListView Grid.Column="0" DataContext="{Binding FileList}"/>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        <ContentControl
            Grid.Column="2"
            Style="{StaticResource ListPlayerStyle}"/>
    </Grid>
</UserControl>
